// @fontsource/* packages ship CSS only (no type declarations). Declare the
// scope so side-effect imports like `import "@fontsource/lobster"` type-check
// under vp's type-aware lint (otherwise TS2882). Mirrors src/types/mdx.d.ts.
declare module "@fontsource/*";
