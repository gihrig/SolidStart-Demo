# Test-only fixtures loaded by vitest (Vite mode "test").
# Non-secret demo credentials — committed so unit tests pass on any machine/CI.
# Real secrets belong in .env.local / .env.test.local (git-ignored).
VITE_TEST_USERNAME=demo1
VITE_TEST_PASSWORD=welcome
